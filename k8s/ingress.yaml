apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: orgconnect-ingress
  namespace: orgconnect
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
  labels:
    app: orgconnect
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: orgconnect-active
            port:
              number: 80
  # Uncomment and configure for custom domain
  # - host: orgconnect.yourdomain.com
  #   http:
  #     paths:
  #     - path: /
  #       pathType: Prefix
  #       backend:
  #         service:
  #           name: orgconnect-active
  #           port:
  #             number: 80

